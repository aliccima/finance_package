[tool.poetry]
name = "finance-package"
version = "0.1.0"
description = ""
authors = ["aliccima <alice.cima@usi.ch>"]
readme = "README.md"
license = "MIT"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
